from typing import Union
from pathlib import Path
from pgimri.dtip.convert import dtitk_to_fsl_multi


def transform_multi(input_path: Union[Path, str],
                    ref: Union[Path, str],
                    output_path: Union[Path, str]) -> int:
    """
    
    """
